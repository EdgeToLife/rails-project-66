= simple_form_for(@new_repository) do |f|
  .form-inputs
    = f.input :github_id, label: t('repository'), collection: @repository_list.map { |repo| [repo.full_name, repo.id] }, required: true
  p
  .form-actions
    = f.button :submit, t('.create_repository'), class: 'btn btn-primary'
